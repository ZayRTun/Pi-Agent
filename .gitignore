# --- Secrets: never commit ---
agent/auth.json

# --- Runtime data / caches / logs (regenerable) ---
agent/npm/
agent/sessions/
agent/mcp-cache.json
agent/mcp-npx-cache.json
agent/pi-crash.log
agent/run-history.jsonl
.pi-subagents/artifacts/

# --- Memory sqlite db (regenerated; the .md memory files ARE committed) ---
agent/pi-hermes-memory/sessions.db
agent/pi-hermes-memory/sessions.db-shm
agent/pi-hermes-memory/sessions.db-wal
agent/.pi-hermes-locks.sqlite*
agent/pi-hermes-memory/.consolidation-locks/

# --- Upstream clone inside prime-tui (re-cloneable) ---
agent/prime-tui/prime-agent-src/

# --- OS noise ---
.DS_Store
