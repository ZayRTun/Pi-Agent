In this pi setup, builtin subagents (worker/scout/planner) have `inheritSkills: false` by default, so delegated tasks must include the relevant skill contract, artifact paths, acceptance criteria, and validation commands explicitly. In pi-subagents, AGENTS.md delegation rules are advisory: the extension exposes a callable subagent tool but does not automatically delegate or police primary repository tools. Reliable mandatory delegation requires a primary-side runtime guard (e.g., a global extension intercepting tool_call). <!-- created=2026-08-06, last=2026-08-07 -->
§
In goldenland-realestate-v2, the issue-tracker workflow is a user preference — missing it gets called out: keep a clean working tree, run the full test suite, commit with a clear message, then close the ticket via `gh issue close` with a summary comment, append a context pointer (e.g., the map's 'Implementation Decisions' section) to the parent issue, and commit the review ledger. The code-review skill reviews uncommitted work against a fixed point (HEAD for uncommitted changes) with the originating issue as spec source; findings go in `docs/agents/reviews/<issue-number>.md`. `.pi-subagents/` is pi-generated noise and must be gitignored. <!-- created=2026-08-06, last=2026-08-07 -->
§
In goldenland-realestate-v2, the bilingual Content Suggestion lifecycle (ticket #8): English lane uses `status`/`accepted_at`; Burmese lane uses `my_accepted_at`/`my_error` columns with model helpers `hasBurmeseTranslation()`/`isBurmeseAccepted()`; use `ContentSuggestionStatus` enum for status. Complex titles stay English-only (Burmese translation echoes the English title); Property adds Burmese title + sub-description. `title_searchable_en` is only written when the English title is accepted; Burmese-only acceptance does not change it. Translation contract: `ContentTranslationAgent::translate(array $english, Property|Complex $target): array`, implemented by `OpenRouterContentTranslationAgent`. The OpenAI generator's budget estimation must use explicit `imageCount`, not `count($content) - 1` (user relies on this). `ContentSuggestionValidator` is the shared base class for `PropertySuggestionValidator` and `ComplexSuggestionValidator`, enforcing the same output contract: fixed field set, plain-text title/meta rejecting entity-encoded markup, 60/160/3000 limits, restricted safe-HTML descriptions. <!-- created=2026-08-07, last=2026-08-07 -->
§
In goldenland-realestate-v2 tests, the `aiAssistAdminUser()` helper registers a `Gate::before` callback that denies all other users; calling it twice in the same test registers two callbacks causing AuthorizationException. Call it only once per test. <!-- created=2026-08-07, last=2026-08-07 -->
§
Network to GitHub API can flap with 'connection reset by peer' and timeout errors. When this happens, retry with backoff and resume operations; the user will prompt a retry if needed. These are transient and not indicative of permanent failures. <!-- created=2026-08-07, last=2026-08-07 -->
§
In goldenland-realestate-v2, ticket #7 closed the Complex English Synthesis gap: the Complex pipeline existed from earlier tickets; the work added a shared ContentSuggestionValidator base, strict contract rejection on failed Complex output, and a provider-boundary test replacement (removed a fragile chain-test heuristic). <!-- created=2026-08-07, last=2026-08-07 -->
§
When running the code-review skill in goldenland-realestate-v2, the review found and fixed: an unused helper (YAGNI), and validator duplication between Property/ComplexSuggestionValidator (extracted into a shared ContentSuggestionValidator base). Always check for dead code and near-verbatim duplication after refactors. <!-- created=2026-08-07, last=2026-08-07 -->
§
The ContentSuggestion model uses Spatie HasTranslations, so JSON columns are already per-locale (['en' => ..., 'my' => ...]); the bilingual lifecycle added `my_accepted_at` and `my_error` columns for the Burmese lane while English keeps `status`/`accepted_at`. <!-- created=2026-08-07, last=2026-08-07 -->
§
In goldenland-realestate-v2, the review ledger for #8 (Bilingual Suggestion States) and #9 (Property Image Evidence) confirm the established workflow: implement, run /code-review, fix findings, commit, then append a context pointer bullet to the parent map issue #1's 'Implementation Decisions' section, and close the ticket with a summary comment. The review for #8 surfaced an off-spec refactor (synthesis budget estimation changed from `getMedia('galleries')->count()` to `count($content) - 1`); it was reverted by passing `imageCount` explicitly. Always verify refactors preserve original behavior, especially around budget counting. <!-- created=2026-08-07, last=2026-08-07 -->