In this pi setup, builtin subagents (worker/scout/planner) have `inheritSkills: false` by default, so delegated tasks must include the relevant skill contract, artifact paths, acceptance criteria, and validation commands explicitly. In pi-subagents, AGENTS.md delegation rules are advisory: the extension exposes a callable subagent tool but does not automatically delegate or police primary repository tools. Reliable mandatory delegation requires a primary-side runtime guard (e.g., a global extension intercepting tool_call). <!-- created=2026-08-06, last=2026-08-07 -->
§
In goldenland-realestate-v2, the issue-tracker workflow is a user preference — missing it gets called out: keep a clean working tree, run the full test suite, commit with a clear message, then close the ticket via `gh issue close` with a summary comment; append a context pointer to parent issue #1's 'Implementation Decisions' section (format: commit SHA, review ledger path, issue closure date). The code-review skill runs as a mandatory step before committing — two-axis review (Standards/Spec) in parallel sub-agents, findings fixed before commit; it reviews uncommitted work against a fixed point (HEAD) with the originating issue as spec source, findings saved to `docs/agents/reviews/<issue-number>.md`. `.pi-subagents/` is pi-generated noise and must be gitignored. <!-- created=2026-08-06, last=2026-08-07 -->
§
Network to GitHub API can flap with 'connection reset by peer' and timeout errors. When this happens, retry with backoff and resume operations; the user will prompt a retry if needed. These are transient and not indicative of permanent failures. <!-- created=2026-08-07, last=2026-08-07 -->
§
In goldenland-realestate-v2, the bilingual Content Suggestion lifecycle (tickets #7/#8, complete): English lane uses `status`/`accepted_at`; Burmese lane uses `my_accepted_at`/`my_error` with model helpers `hasBurmeseTranslation()`/`isBurmeseAccepted()`; use `ContentSuggestionStatus` enum. The model uses Spatie HasTranslations, so JSON columns are already per-locale (['en'=>…,'my'=>…]). Complex titles stay English-only (Burmese echoes the English title); Property adds Burmese title + sub-description. `title_searchable_en` is only written when the English title is accepted; Burmese-only acceptance doesn't change it. Translation runs through `ContentTranslationAgent::translate(array $english, Property|Complex $target): array` implemented by `OpenRouterContentTranslationAgent`. The OpenAI generator's budget estimation must use explicit `imageCount`, not `count($content) - 1` (user relies on this; a refactor swapping it in for #8 was reverted — always verify refactors preserve original behavior). `ContentSuggestionValidator` is the shared base for Property/Complex validators enforcing the output contract: fixed field set, plain-text title/meta rejecting entity-encoded markup, 60/160/3000 limits, restricted safe-HTML descriptions. Testing note (ticket #8): `aiAssistAdminUser()` registers a `Gate::before` callback denying all other users; calling it twice in the same test registers two callbacks, causing AuthorizationException on the first `Gate::authorize` — call it exactly once per test (even in nested describes). <!-- created=2026-08-07, last=2026-08-07 -->
§
In goldenland-realestate-v2, ticket #9 (Property Image Evidence): adds an `ImageObservation` model and `image_observations` table, `media_evidence` columns on `content_suggestions`, an `ImageObservationAgent` contract with `OpenRouterImageObservationAgent` implementation, and an `ImageObservationService`. The glossary defines Image Observation as supporting evidence (not verified facts) — preserve this when implementing media evidence and validate against the acceptance criteria before considering it complete. Filament/Livewire gotchas: the selection modal replaces the confirmation step in Filament actions (confirmation modal skips the form schema), so a custom form in the action modal must be the primary modal with warnings in the modal description; calling `callAction` on a Livewire component leaves the action mounted, causing ActionNotResolvableException on a subsequent `mountAction` — restructure tests to use a fresh component instance. <!-- created=2026-08-07, last=2026-08-07 -->