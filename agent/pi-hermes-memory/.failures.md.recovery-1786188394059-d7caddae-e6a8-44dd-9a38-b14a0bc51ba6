pi-hermes custom-footer.ts: don't drop existing functionality when adding features — removing the total-context fraction (85k/200k) to fit I/O tokens was called out and had to be restored. For live I/O token counts: ctx.getContextUsage() has no input/output split and getBranch() misses compaction/branch_summary usage — use getEntries() plus a message_update listener estimating output tokens from accumulated chars/4, cleared on message_end. <!-- created=2026-07-29, last=2026-08-07 -->
§
GitHub ops (gh CLI) in goldenland-realestate-v2: issues track work but are not auto-closed when implementation lands — verify ticket state against committed code and close stale tickets before proceeding. Network to api.github.com (gh CLI or inline curl) flaps with 'connection reset by peer' / 'Operation timed out' — retry with backoff and resume; after transient failures subsequent attempts succeed. <!-- created=2026-08-06, last=2026-08-08 -->
§
Laravel/Eloquent (goldenland-realestate-v2): when adding a trait to a model, add both the import and the `use TraitName;` in the class body — missing either causes BadMethodCallException ('Call to undefined method'). Pint prunes unused imports, so land both edits together and verify after formatting. <!-- created=2026-08-06, last=2026-08-07 -->
§
Spatie HasTranslations (goldenland-realestate-v2): a missing locale reads back as '' (empty string), not null — when asserting an absent translation, check raw attribute storage or getTranslations() locale keys rather than null on the accessor. <!-- created=2026-08-06, last=2026-08-07 -->
§
[tool-quirk] When the `edit-failure` message includes 'Could not find edits[0] in <path>', the oldText must match exactly including whitespace and newlines; always verify the exact current file content before replacing. <!-- created=2026-08-06, last=2026-08-06 -->
§
AGENTS.md policy: discretionary wording such as 'delegate when doing so keeps the primary context healthy' did not reliably trigger subagent use, and an installed graphify skill with a graphify-out knowledge graph was never invoked by the primary. Effective policy needs mandatory, observable triggers: a default worker for substantive implementation, a narrow trivial-task exception, and an explicit preflight to run /graphify query for repository-understanding questions before generic scouting. <!-- created=2026-08-06, last=2026-08-07 -->
§
goldenland-realestate-v2 Filament v4.12 / Livewire 3.8 test harness: exceptions thrown inside a component action are NOT rethrown to the test — HttpException/AuthorizationException become a swallowed 403 (no assertForbidden/assertStatus here) and ValidationException surfaces as an error bag; assert the security outcome (record unchanged) instead of expectException. Also: callAction merges list state element-wise (checkbox arrays get wrong values — use a direct set(statePath, value)) and leaves the action mounted (a later mountAction on the same component throws ActionNotResolvableException — use a fresh component). <!-- created=2026-08-06, last=2026-08-07 -->
§
goldenland-realestate-v2 ticket #8 permission tests: register aiAssistAdminUser()/propertyProviderFake() exactly once per test — each call adds a Gate::before callback denying all other users, and duplicate registration (nested describes) throws AuthorizationException. Also guard acceptTranslation() against suggestions without a Burmese translation (else LogicException). <!-- created=2026-08-07, last=2026-08-07 -->
§
goldenland-realestate-v2 reviews caught off-spec refactors: (1) budget estimation must keep original semantics — pass imageCount explicitly, not count($content) - 1 or getMedia('galleries')->count(); (2) the OpenRouter content agent must forward observations it receives to the generator (dropping them is silent data loss) — add a wiring test. <!-- created=2026-08-07, last=2026-08-07 -->
§
goldenland-realestate-v2: Spatie getMedia() caches per model instance — media deleted mid-test still appears in the cached collection; use a fresh instance ($model->fresh()) to observe post-delete state. In-memory sqlite tests: ImageObservation.media_id FK fails unless the Spatie media row is persisted first. <!-- created=2026-08-07, last=2026-08-08 -->
§
[tool-quirk] memory tool 'replace' swaps the WHOLE entry — `content` must be the complete new entry text, not a diff. Passing only a changed snippet clobbers the rest of the entry (hit this during a memory consolidation). Reconstruct full text before replacing. <!-- created=2026-08-07, last=2026-08-07 -->
§
[correction] no, I am not talking about the progress bar's width. I am talking about reducing the height of the progress bar. — Failed: User corrected the agent <!-- created=2026-08-08, last=2026-08-08, project64=LnBp -->
§
[tool-quirk] Bash shell throws `/bin/bash: [0-9a-f]+: command not found` when an assistant tries to run a command with a regex pattern that isn't a valid command; avoid using bare regex as a command and ensure grep/sed patterns are properly quoted or use proper tools. <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->
§
[insight] Fixed-point pinning for batched close-out reviews of multiple tickets: when /code-review is run without a fixed point over a run of feature commits, pin per-ticket from the commit graph (feature commit SHA and its parent). Parent SHA is the fixed point; review diff is `git diff <parent>...<feature>`. Feature commits identified by matching commit subject to issue title. Ledger fixed-point line records HEAD-before-work + note that work reviewed as uncommitted diff. — Failed: Initial /code-review invocation had no fixed point; resolved by pinning per-ticket in later turn. <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->
§
[tool-quirk] pi-subagents async runs: aggregate returns can be truncated and a child can fail transiently ('Model is unavailable' / 400 server_error from Console Go/opencode-go/deepseek-v4-flash) even when the parent is fine — re-run only the failed child and wait. Child-run IDs aren't always in status.json steps; map runs to tickets via .pi-subagents/artifacts/<run-id>_<agent>_input.md files and read full outputs from the _output.md artifacts. <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->
§
[tool-quirk] Model availability can transiently fail for sub-agents (error 'Model is unavailable' from provider Console Go) — even when the parent session is fine. When a parallel fan-out has one failed child, re-run only that failed axis and wait for it; other children's outputs are already valid. — Failed: In the 6-agent review batch, std-6 failed with 'Model is unavailable' while 5 others succeeded; re-ran just std-6. <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->
§
[tool-quirk] pi-subagents async workflow run: results may be truncated and a child subagent can fail transiently (e.g., 'Model is unavailable' from Console Go/opencode-go/deepseek-v4-flash). Always inspect status.json and child artifacts under .pi-subagents/artifacts/<runId>/ to get full outputs; map artifacts to tickets via the *_input.md files. Re-run failed children individually. Child run IDs are not always present in status.json steps, so correlate via input files. — Failed: Encountered during batched 6-subagent code review close-out: aggregate return truncated and std-6 failed with transient provider error. <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->
§
[tool-quirk] Sub-agent workflow runs for code reviews: aggregate returns get truncated and children can fail with transient provider errors ('Model is unavailable', 400 server_error from Console Go). Always inspect raw outputs in .pi-subagents/artifacts/ and re-run failed children; map artifacts to tickets via input files (child-run IDs aren't in status.json). <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->
§
[tool-quirk] Filament v4.12 has `assertActionVisible`/`assertActionHidden` testing helpers, and `assertNotified`/`assertNotNotified` exist for notifications. Ripgrep with `-r` flag mangles output (shows as `->n(`), so avoid using `-r` when searching test files. — Failed: Tool misuse during test exploration; useful convention for future test writing. <!-- created=2026-08-08, last=2026-08-08, project64=Z29sZGVubGFuZC1yZWFsZXN0YXRlLXYy -->