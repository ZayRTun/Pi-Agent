goldenland-realestate-v2 AI Assist work (#1–#10; workflow part is a user preference — missing it gets called out):
- Workflow: clean working tree → full test suite → commit with clear message → `gh issue close` with summary comment → append context pointer (commit SHA, review ledger path, closure date) to parent issue #1's 'Implementation Decisions' section. Code-review skill is a mandatory pre-commit step: two-axis (Standards/Spec) review in parallel sub-agents against HEAD with the originating issue as spec; fix findings first, save to `docs/agents/reviews/<issue-number>.md`, wait for reviewer before committing. Findings are separate actionable items (dead test helpers removed, duplicated validators extracted into shared base, off-spec reverted); reviewer may catch real blockers. `.pi-subagents/` is pi-generated noise — gitignore. Mirror existing implementations for new tickets (e.g., an existing OpenRouter agent for Complex reused for Property).
- Pipeline: shared `ContentSuggestionValidator` base for Property/Complex (fixed field set; plain-text title/meta rejecting entity-encoded markup; 60/160/3000 limits; restricted safe-HTML descriptions); Complex never carries `sub_description`, even on failure.
- Bilingual (#8): en/my lanes on same row (`my_accepted_at`/`my_error` on `content_suggestions`), helpers `hasBurmeseTranslation()`/`isBurmeseAccepted()`, enum `ContentSuggestionStatus`; English synthesized first; Spatie HasTranslations = JSON cols per locale; Complex title stays English-only (Property adds Burmese title + sub-description); `title_searchable_en` written only on English acceptance; `ContentTranslationAgent::translate(array $english, Property|Complex $target): array` → OpenRouter impl; budget estimation uses explicit `imageCount`.
- ImageObservation (#9–#10): model + `image_observations` table, `media_evidence` columns, `ImageObservationService::ensure()` reuses stored non-failed observation else calls `ImageObservationAgent` (OpenRouter impl, strict {usable, description, error} schema); glossary: Image Observation = supporting evidence, never verified facts; `media_id` FKs `media` — in-memory sqlite fails FK unless media persisted; Spatie caches `getMedia` per instance — use fresh instance; failures → danger Filament notification (`Image observations failed for <filename>`), `error` column, no suggestion created. Selection via gallery-image CheckboxList, shared through `ManagesContentSuggestions` trait (default for both pages); only `galleries` media counts — logo never counts as required evidence; Complex requires ≥1 usable selected gallery image for image-supported generation; `withLogo()` factory state for tests.
- Testing gotchas: `aiAssistAdminUser()`/`propertyProviderFake()` register `Gate::before` denying other users — call exactly once (incl. nested `beforeEach` → AuthorizationException); keep helpers idempotent. Filament modals: confirmation modal replaces form step — custom action form must be primary modal; `callAction` leaves action mounted → ActionNotResolvableException on later `mountAction` — use fresh component instance. Set checkbox arrays via `set`(statePath, value); assert on state, not HTML. Deterministic agents ignore observations in generate() — mark with a `ponytail:` note in docblock to signal intentional behavior vs. subagent context. <!-- created=2026-08-07, last=2026-08-07 -->
§
Goldenland Real Estate AI Assist ticket #10: Complex synthesis now uses the same selectable, reusable image-observation path as Property (#9). Complex edit-page AI Assist modal has a gallery-image CheckboxList (moved into shared ManagesContentSuggestions trait as default for both pages); only `galleries` media counts — logo never counts as required content evidence; Complex requires at least one usable selected gallery image for image-supported generation; new `withLogo()` factory state for tests. OpenRouterComplexContentAgent passes observations to generator; DeterministicComplexContentAgent ignore observations marked with ponytail note. <!-- created=2026-08-07, last=2026-08-07 -->
§
Goldenland Real Estate AI Assist ticket #9: Property synthesis grounded on reusable image observations. New ImageObservation model/table keyed by media_id (a Spatie media row is the version); ImageObservationService::ensure() reuses stored non-failed observation else calls ImageObservationAgent (OpenRouter impl, strict {usable, description, error} schema). Property edit-page AI Assist modal has a gallery-image CheckboxList for selection; media_evidence JSON on suggestions; failed observations → danger notification ('Image observations failed for <filename>'), error column, no suggestion created. Pinned tests: in-memory sqlite FK fails unless media persisted; Spatie caches getMedia per instance — use fresh instance to see deletions; set checkbox arrays via set not fillForm. <!-- created=2026-08-07, last=2026-08-07 -->