goldenland-realestate-v2 AI Assist work (#1–#11; workflow part is a user preference — missing it gets called out): <!-- created=2026-08-07, last=2026-08-07 -->
§
Goldenland Real Estate AI Assist ticket #7: Complex synthesis already existed; this closed remaining Complex-specific gaps. ContentSuggestionValidator is now a shared base class for Property/Complex — both enforce fixed field set, plain-text title/meta (rejecting entity-encoded markup), 60/160/3000 limits, restricted safe-HTML descriptions; Complex never carries sub_description even on failure. Privacy tests added; provider instruction target-aware (Complex prompt doesn't claim Property sub-description items). <!-- created=2026-08-07, last=2026-08-07 -->
§
For goldenland-realestate-v2, executing the implement skill per ticket requires loading the originating issue via gh and reading the 'Parent' section plus acceptance criteria; the two-axis code review via parallel sub-agents is mandatory before commit, and the resolve step (`gh issue close` with summary, append pointer to #1's 'Implementation Decisions') must not be skipped. <!-- created=2026-08-07, last=2026-08-07 -->
§
ticket #7 (goldenland-realestate-v2): ComplexSuggestionValidator now extends shared ContentSuggestionValidator base; provider prompt made target-aware (Complex doesn't claim Property sub-description items). The two-axis review caught an unused helper (YAGNI) and near-duplicate validator twins, prompting extraction into one base class. <!-- created=2026-08-07, last=2026-08-07 -->
§
ticket #8 (goldenland-realestate-v2): Bilingual suggestion states — English lane uses existing status/accepted_at; Burmese lane adds my_accepted_at + my_error columns. Property title/content translates (Burmese title/sub_description added), Complex description/meta translates while Complex title stays English-only. New ContentTranslationAgent contract with OpenRouter implementation. Requires 'Translate:ContentSuggestion' permission. <!-- created=2026-08-07, last=2026-08-07 -->
§
test quirk (goldenland-realestate-v2): calling aiAssistAdminUser() twice in a test registers two Gate::before callbacks — the first denies any other user. Call it once per test. Also: Spatie caches getMedia per model instance; deleted media stays in cached collection — use a fresh model instance after media deletion in tests. <!-- created=2026-08-07, last=2026-08-07 -->
§
ticket #9 (goldenland-realestate-v2): Property synthesis grounds on reusable Image Observations. New ImageObservation table keyed by media_id (Spatie media row = version), ImageObservationService::ensure() reuses stored non-failed observations, ImageObservationAgent contract + OpenRouterImageObservationAgent (strict usable/description/error shape). PropertyContentAgent::generate now takes `array $observations = []`. Property becomes text-only (no raw images sent). Review caught OpenRouterPropertyContentAgent dropping observations — wire them through. <!-- created=2026-08-07, last=2026-08-07 -->
§
Filament test quirk (goldenland-realestate-v2): Filament's test helper fillForm/mergeState merges list state element-wise, so checkbox arrays need a direct `set` instead. Notification body matching should use Notification instance assertions; evidence inside a review modal holds in component state but doesn't render server-side into modal HTML — assert on state, not HTML. <!-- created=2026-08-07, last=2026-08-07 -->
§
ticket #10 (goldenland-realestate-v2): Complex synthesis now uses the same selectable/reusable image-observation path as Property. Selection CheckboxList moved into shared ManagesContentSuggestions trait as default for both pages. ComplexContentAgent::generate gained `array $observations = []`; deterministic agent carries a ponytail: note for ignoring observations. Logos never count as required content evidence (no-invent constraint). <!-- created=2026-08-07, last=2026-08-07 -->
§
ticket #11 (in progress, goldenland-realestate-v2): adding deterministic targeted web-search phase. New SearchAnchor value object (privacy-safe phrase + official domain), WebSearchAgent contract with OpenRouter implementation (web_search tool, citations surfaced on GenerationResponse), WebEvidenceService orchestrator, web_evidence JSON on suggestions. WebEvidenceService defaults deduped onto AiConfiguration constants. ContentSuggestionService::generate now also takes web evidence; add webSearchFake() in test setup wherever generate() runs. <!-- created=2026-08-07, last=2026-08-07 -->