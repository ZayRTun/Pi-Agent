pi-hermes custom-footer.ts: don't drop existing functionality when adding features — removing the total-context fraction (85k/200k) to fit I/O tokens was called out and had to be restored. For live I/O token counts: ctx.getContextUsage() has no input/output split and getBranch() misses compaction/branch_summary usage — use getEntries() plus a message_update listener estimating output tokens from accumulated chars/4, cleared on message_end. <!-- created=2026-07-29, last=2026-08-07 -->
§
[failure] I/O token counters in custom-footer.ts were static during a turn because they relied on `ctx.sessionManager.getBranch()` entries. Solved by adding a `message_update` event listener that estimates output tokens from accumulated text length (chars/4) during streaming, falling back to completed entries after `message_end` clears the estimate. — Failed: ctx.getContextUsage() returns only {tokens, contextWindow, percent} — no input/output split <!-- created=2026-07-29, last=2026-07-29 -->
§
[correction] getBranch() missed compaction/branch_summary usage entries. Fixed by switching to getEntries() which also captures those. — Failed: getBranch() only includes current branch entries, missing compaction summaries <!-- created=2026-07-29, last=2026-07-29 -->
§
GitHub ops (gh CLI) in this repo: issues track work but are not auto-closed when implementation lands — verify ticket state against committed code and close stale tickets before proceeding. Network to api.github.com flaps with 'connection reset by peer' and timeouts; retry with backoff and resume. <!-- created=2026-08-06, last=2026-08-07 -->
§
Laravel/Eloquent (goldenland-realestate-v2): when adding a trait to a model, add both the import and the `use TraitName;` in the class body — missing either causes BadMethodCallException ('Call to undefined method'). Pint prunes unused imports, so land both edits together and verify after formatting. <!-- created=2026-08-06, last=2026-08-07 -->
§
[tool-quirk] When adding a trait import to a Laravel model, must also add the class `use` statement; otherwise methods are undefined at runtime (BadMethodCallException). Also, pint may remove the class `use` if only the import was added, so verify both after formatting. <!-- created=2026-08-06, last=2026-08-06 -->
§
Spatie HasTranslations (goldenland-realestate-v2): a missing locale reads back as '' (empty string), not null — when asserting an absent translation, check raw attribute storage or getTranslations() locale keys rather than null on the accessor. <!-- created=2026-08-06, last=2026-08-07 -->
§
[tool-quirk] When the `edit-failure` message includes 'Could not find edits[0] in <path>', the oldText must match exactly including whitespace and newlines; always verify the exact current file content before replacing. <!-- created=2026-08-06, last=2026-08-06 -->
§
[correction] When implementing ticket #3 in goldenland-realestate-v2, a first attempt to use `HasContentSuggestions` trait on Complex/Property models failed at runtime with 'Call to undefined method contentSuggestions()' because the trait import was added but the class `use` statement was not. Fix: ensure both the `use` import and the trait in the class body are added. Also, spatie's `HasTranslations` returns `''` for missing locales, not null; assert on raw attribute storage or locale existence rather than null when checking absent translations. <!-- created=2026-08-06, last=2026-08-06 -->
§
AGENTS.md policy: discretionary wording such as 'delegate when doing so keeps the primary context healthy' did not reliably trigger subagent use, and an installed graphify skill with a graphify-out knowledge graph was never invoked by the primary. Effective policy needs mandatory, observable triggers: a default worker for substantive implementation, a narrow trivial-task exception, and an explicit preflight to run /graphify query for repository-understanding questions before generic scouting. <!-- created=2026-08-06, last=2026-08-07 -->
§
[tool-quirk] A graphify skill can be installed and a repository can contain `graphify-out/graph.json` without the primary invoking it; the skill description alone did not create a reliable trigger. AGENTS.md needs an explicit preflight: for repository-understanding questions, check the project-root map and run `/graphify query` before ordinary scout reconnaissance. — Failed: The primary followed generic scout routing and did not proactively initiate the graphify skill's fast path. <!-- created=2026-08-06, last=2026-08-06 -->
§
goldenland-realestate-v2 Filament v4.12 / Livewire 3.8 test harness: exceptions thrown inside a component action are NOT rethrown to the test — HttpException/AuthorizationException become a swallowed 403 (no assertForbidden/assertStatus here) and ValidationException surfaces as an error bag; assert the security outcome (record unchanged) instead of expectException. Also: callAction merges list state element-wise (checkbox arrays get wrong values — use a direct set(statePath, value)) and leaves the action mounted (a later mountAction on the same component throws ActionNotResolvableException — use a fresh component). <!-- created=2026-08-06, last=2026-08-07 -->
§
[correction] In goldenland-realestate-v2, ticket #8 tests must not call `aiAssistAdminUser()` twice in the same test — each call registers a `Gate::before` callback denying all other users, causing AuthorizationException. Call it exactly once per test (even in nested describes, ensure only one registration). — Failed: Repeated `aiAssistAdminUser()` in permission tests caused AuthorizationException due to duplicate Gate::before callbacks. <!-- created=2026-08-07, last=2026-08-07 -->
§
[insight] In goldenland-realestate-v2, refactoring OpenAI generator's budget estimation from `count($content) - 1` to `getMedia('galleries')->count()` was an off-spec change; the correct approach is to pass `imageCount` explicitly. Always preserve existing semantics when extracting shared logic. — Failed: Review found the budget estimation change was off-spec; fixed by reverting to explicit imageCount. <!-- created=2026-08-07, last=2026-08-07 -->
§
[correction] In goldenland-realestate-v2, the code review for #8 flagged that a refactor unintentionally changed the OpenRouter budget estimation from explicit image count to counting content array items; reverted by passing `imageCount` explicitly. This is a correction on how to handle the budget. — Failed: User correction on budget estimation approach. <!-- created=2026-08-07, last=2026-08-07 -->
§
[tool-quirk] The GitHub API occasionally flaps with 'connection reset by peer' (on POST to api.github.com/graphql) and timeout errors. Retry with backoff and resume operations; user prompts retry when needed. — Failed: Transient network failure during gh issue operations for ticket 9; not a permanent failure. <!-- created=2026-08-07, last=2026-08-07 -->
§
[tool-quirk] GitHub connections can flap with `connection reset by peer` / timeouts; retrying with backoff eventually succeeds. <!-- created=2026-08-07, last=2026-08-07 -->
§
[correction] In goldenland-realestate-v2 ticket #8, the double `aiAssistAdminUser()`/`propertyProviderFake()` in nested describe blocks registered two `Gate::before` callbacks, causing AuthorizationException — call exactly once per test to avoid state pollution. Also, `acceptTranslation()` must guard against suggestions without a Burmese translation (else LogicException). — Failed: Duplicate Gate callbacks from nested beforeEach; missing guard <!-- created=2026-08-07, last=2026-08-07 -->
§
[insight] Goldenland-realestate-v2 review caught an off-spec refactor in ticket #8: synthesis budget estimation mistakenly used `count($content) - 1` instead of the original `getMedia('galleries')->count()` — revert to explicit `imageCount` parameter. Also in ticket #9, review caught `OpenRouterPropertyContentAgent` dropping the observations it receives (silent data loss) — always forward observations to the generator and add a wiring test. — Failed: Off-spec refactor and silent data-drop caught by two-axis review <!-- created=2026-08-07, last=2026-08-07 -->
§
[tool-quirk] Goldenland-realestate-v2 testing with GitHub via `gh` CLI and inline `curl`: network to api.github.com flaps with 'connection reset by peer' and 'Operation timed out' — retry with backoff; after transient failures, subsequent attempts succeed. Also, the review budget-tracking in ticket #8 showed that Spatie `getMedia` caches per instance — deleted media still appears in test collections; use a fresh instance. — Failed: Flaky GitHub network and Spatie getMedia caching <!-- created=2026-08-07, last=2026-08-07 -->
§
[tool-quirk] In goldenland-realestate-v2, Filament test helper `callAction` merges list state element-wise, so passing a checkbox array in `callAction` data results in wrong values — use a direct `set`(statePath, value) on the Livewire component instead. Additionally, `callAction` leaves the action mounted, so a subsequent `mountAction` on the same component causes ActionNotResolvableException — use a fresh component instance for follow-up actions. <!-- created=2026-08-07, last=2026-08-07 -->
§
[tool-quirk] goldenland-realestate-v2 in-memory sqlite tests: `ImageObservation.media_id` FK fails unless the Spatie media row is persisted. Spatie caches `getMedia()` per model instance — deleting media during a test leaves stale entries in the cached collection; use a fresh model instance (`$model->fresh()`) to see the post-delete state. <!-- created=2026-08-07, last=2026-08-07 -->