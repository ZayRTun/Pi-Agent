Goldenland AI Assist (USER PREFERENCE — missing it gets called out): per implement ticket, load the originating issue via gh, read its 'Parent' + acceptance criteria first. Two-axis code review (standards + spec) via parallel delegate/reviewer sub-agents is mandatory before commit — they inherit the session model and have shell access in the repo cwd; no specialized code-review subagent exists — pass explicit shell diff instructions (`git diff <fixed-point>...HEAD`, `git log <fixed-point>..HEAD`). Code-review skill needs an explicit fixed point (SHA/tag/branch/range) — else pin it per-ticket from the commit graph (feature commit's parent SHA). Ledger to docs/agents/reviews/<issue>.md: fixed point, spec source, standards sources, findings table (finding/axis/classification/disposition). Verify findings against HEAD before writing ledgers — later batch tickets may already fix them; distinguish resolved-by-later-commit vs still-open-at-HEAD. Delete dead code only with zero references at HEAD, excluding .pi-subagents/ and docs/agents/reviews/. Never skip Resolve: `gh issue close` with summary (commit SHA, test results, ledger path), appending a context-pointer comment (not a separate file) to #1's 'Implementation Decisions'. Batched close-outs: run 2 axes × N tickets as parallel sub-agents (unlimited spawn budget); trust nothing from workflow completion events (truncated aggregates, transient 'Model is unavailable' child failures) — inspect raw outputs in .pi-subagents/artifacts/, re-run failed children. Testing: fixes that change behavior require updating existing tests that encoded the old contract (e.g. GA4 health tests needed ga4_key_events after gating health on key events); regression tests go alongside existing tests (DomainBlacklistServiceTest, Ga4HealthTest). Model behavior: free models work at $0 balance — only a negative balance blocks (402s everything incl. free); free variants report explicit 0/0 pricing in /models, so the budget guard should skip free models; OpenRouter bills after completion, so balance can dip negative up to a limit — hence the warning's lead time. <!-- created=2026-08-08, last=2026-08-08 -->
§
Pi environment facts: Pi auto-loads every .ts/.js directly in ~/.pi/agent/extensions/ (and cwd/.pi/extensions/) in addition to the settings.json 'extensions' array; subdirs load only with index.ts or package.json carrying pi.extensions — disable an extension by moving it to a subdir (e.g. extensions/retired/). prime-tui patches: reapply `bash ~/.pi/agent/prime-tui/apply-patches.sh` after any npm update of pi-coding-agent; patched copies live in ~/.pi/agent/prime-tui/patched/. prime-header.ts: splash header (logo + version/model/cwd/counts) via ctx.ui.setHeader on session_start. Patched TUI quirks: settings-list.js — Space always toggles the selected option (stock only toggles with empty search) and strips spaces from search input; interactive-mode.js — fullscreen quit fixed via TUI stop with preserveScreen: true; editor.js — regular-mode overflow crash (TuiMainScreen.doRender throws on any line wider than the terminal, writes pi-crash.log) was caused by '> ' prompt drawn inside left padding without shrinking the text budget (lines 2 cols too wide); fullscreen never checks, so only regular mode crashes. pi-subagents extension v0.42.1 (Nico Bailon): intercom bridge, 8 builtin agents, 35 skills, unlimited spawn budget. <!-- created=2026-08-08, last=2026-08-08 -->
§
User collaboration & design-session style: design stress-testing via /grill-with-docs and /grilling skills; prefers a 'design tree' with rounds of questions on the current frontier. On context loss user invokes /wait-what — re-pitch in ASD-STE100 Simplified Technical English + ubiquitous language from CONTEXT.md. Goldenland model selection: 'where do we decide which model?' → 'Global model chain' multi-select in AI Settings → 'OpenRouter generation policy' (reorderable, from live catalog filtered to image-input + structured-output models) — the single place the admin decides; free models must be explicitly included in the chain for no-credit fallback. Primary agent delegates context-heavy, independently verifiable work to focused subagents; primary keeps user-facing decisions, orchestration, synthesis, final verification. User prefers global AGENTS.md guidance to stay generic and reusable rather than naming or encoding vendor-specific skill flows (e.g. Matt Pocock's skills); skill-specific procedures belong in their own SKILL.md files. <!-- created=2026-08-08, last=2026-08-08 -->