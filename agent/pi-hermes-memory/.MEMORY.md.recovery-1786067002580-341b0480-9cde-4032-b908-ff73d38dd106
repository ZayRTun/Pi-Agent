In this pi setup, builtin subagents (worker/scout/planner) have `inheritSkills: false` by default, so delegated tasks must include the relevant skill contract, artifact paths, acceptance criteria, and validation commands explicitly. In pi-subagents, AGENTS.md delegation rules are advisory: the extension exposes a callable subagent tool but does not automatically delegate or police primary repository tools. Reliable mandatory delegation requires a primary-side runtime guard (e.g., a global extension intercepting tool_call). <!-- created=2026-08-06, last=2026-08-07 -->
§
In goldenland-realestate-v2, the issue-tracker workflow is a user preference — missing it gets called out: keep a clean working tree, run the full test suite, commit with a clear message, then close the ticket via `gh issue close` with a summary comment; append a context pointer to parent issue #1's 'Implementation Decisions' section (format: commit SHA, review ledger path, issue closure date). The code-review skill runs as a mandatory step before committing — two-axis review (Standards/Spec) in parallel sub-agents, findings fixed before commit; it reviews uncommitted work against a fixed point (HEAD) with the originating issue as spec source, findings saved to `docs/agents/reviews/<issue-number>.md`. `.pi-subagents/` is pi-generated noise and must be gitignored. <!-- created=2026-08-06, last=2026-08-07 -->
§
GitHub API calls can flap with 'connection reset by peer' and timeout errors. Retry with backoff and resume operations; these are transient, not permanent failures. <!-- created=2026-08-07, last=2026-08-07 -->
§
In goldenland-realestate-v2, AI content pipeline (tickets #7–#9): bilingual Content Suggestion lifecycle — English lane uses `status`/`accepted_at`; Burmese lane `my_accepted_at`/`my_error` with helpers `hasBurmeseTranslation()`/`isBurmeseAccepted()`; enum `ContentSuggestionStatus`. Spatie HasTranslations makes JSON columns per-locale. Complex titles stay English-only (Burmese echoes English title); Property adds Burmese title + sub-description; `title_searchable_en` written only when English title is accepted. Translation runs through `ContentTranslationAgent::translate(array $english, Property|Complex $target): array` implemented by `OpenRouterContentTranslationAgent`; budget estimation must use explicit `imageCount`, not `count($content) - 1`. `ContentSuggestionValidator` is the shared base for Property/Complex validators enforcing the output contract (fixed field set, plain-text title/meta rejecting entity-encoded markup, 60/160/3000 limits, restricted safe-HTML descriptions). Ticket #9: `ImageObservation` model + `image_observations` table, `media_evidence` columns, `ImageObservationAgent`→`OpenRouterImageObservationAgent`, `ImageObservationService`; glossary defines Image Observation as supporting evidence (not verified facts). `ImageObservation.media_id` FKs to `media` — in-memory sqlite tests fail the FK when media isn't persisted, so only attach observations to validated/persisted media. <!-- created=2026-08-07, last=2026-08-07 -->
§
Testing/Livewire gotchas: `aiAssistAdminUser()` registers a `Gate::before` callback denying all other users; calling it twice in the same test registers two callbacks, causing AuthorizationException on the first `Gate::authorize` — call it exactly once per test (even in nested describes). The selection modal replaces the confirmation step in Filament actions (confirmation modal skips the form schema), so a custom form in the action modal must be the primary modal with warnings in the modal description; calling `callAction` leaves the action mounted, causing ActionNotResolvableException on a subsequent `mountAction` — restructure tests to use a fresh component instance. <!-- created=2026-08-07, last=2026-08-07 -->