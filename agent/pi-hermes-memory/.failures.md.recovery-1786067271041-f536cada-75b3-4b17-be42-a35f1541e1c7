[correction] When adding a new feature to existing code, do not remove or drop existing functionality (e.g., removed the "85k/200k" context total when adding I/O tokens to the custom-footer). User will notice and call it out. — Failed: Dropped the total context fraction (85k/200k) when refactoring the right column to add I/O token display. User asked "why did you remove the total context" — had to put it back. <!-- created=2026-07-29, last=2026-07-29 -->
§
[failure] I/O token counters in custom-footer.ts were static during a turn because they relied on `ctx.sessionManager.getBranch()` entries. Solved by adding a `message_update` event listener that estimates output tokens from accumulated text length (chars/4) during streaming, falling back to completed entries after `message_end` clears the estimate. — Failed: ctx.getContextUsage() returns only {tokens, contextWindow, percent} — no input/output split <!-- created=2026-07-29, last=2026-07-29 -->
§
[correction] getBranch() missed compaction/branch_summary usage entries. Fixed by switching to getEntries() which also captures those. — Failed: getBranch() only includes current branch entries, missing compaction summaries <!-- created=2026-07-29, last=2026-07-29 -->
§
[tool-quirk] In this repo, GitHub issues track work but are not auto-closed when implementation lands; the frontier query can surface a ticket whose work is already done. The agent should verify issue state against committed code and close stale tickets before proceeding. — Failed: Agent found ticket #2 still OPEN despite its implementation being committed and reviewed; had to close it manually before starting next ticket. <!-- created=2026-08-06, last=2026-08-06 -->
§
[correction] When adding a trait to an Eloquent model, both the `use` import at the top and the `use TraitName;` inside the class body must be added. The assistant initially added the trait to the class body but forgot the import, causing 'Call to undefined method' errors. Pint removes unused imports, so edits that only add the class-level `use` can fail if the import isn't also added simultaneously. — Failed: Forgot to add the import statement when using a new trait in a model; caused BadMethodCallException. <!-- created=2026-08-06, last=2026-08-06 -->
§
[tool-quirk] When adding a trait import to a Laravel model, must also add the class `use` statement; otherwise methods are undefined at runtime (BadMethodCallException). Also, pint may remove the class `use` if only the import was added, so verify both after formatting. <!-- created=2026-08-06, last=2026-08-06 -->
§
[tool-quirk] Spatie's HasTranslations reads a missing locale as `''` (empty string), not null. Assertions on translated fields with no value must check raw attributes or use getTranslations() and verify no locale keys exist, not assert null on the accessor. <!-- created=2026-08-06, last=2026-08-06 -->
§
[tool-quirk] When the `edit-failure` message includes 'Could not find edits[0] in <path>', the oldText must match exactly including whitespace and newlines; always verify the exact current file content before replacing. <!-- created=2026-08-06, last=2026-08-06 -->
§
[correction] When implementing ticket #3 in goldenland-realestate-v2, a first attempt to use `HasContentSuggestions` trait on Complex/Property models failed at runtime with 'Call to undefined method contentSuggestions()' because the trait import was added but the class `use` statement was not. Fix: ensure both the `use` import and the trait in the class body are added. Also, spatie's `HasTranslations` returns `''` for missing locales, not null; assert on raw attribute storage or locale existence rather than null when checking absent translations. <!-- created=2026-08-06, last=2026-08-06 -->
§
[failure] In AGENTS.md, conditional delegation language such as “delegate when doing so keeps the primary context healthy” did not reliably trigger subagent use; the primary implemented most substantive work itself. Effective policy needs mandatory, observable delegation triggers, a default worker for substantive implementation, and a narrow trivial-task exception. — Failed: The primary agent interpreted discretionary delegation guidance as permission to execute repository work inline. <!-- created=2026-08-06, last=2026-08-06 -->
§
[tool-quirk] A graphify skill can be installed and a repository can contain `graphify-out/graph.json` without the primary invoking it; the skill description alone did not create a reliable trigger. AGENTS.md needs an explicit preflight: for repository-understanding questions, check the project-root map and run `/graphify query` before ordinary scout reconnaissance. — Failed: The primary followed generic scout routing and did not proactively initiate the graphify skill's fast path. <!-- created=2026-08-06, last=2026-08-06 -->
§
[tool-quirk] Livewire 3.8 test harness (goldenland-realestate-v2, Filament v4.12): exceptions thrown inside a component action are NOT rethrown to the test — RequestBroker::temporarilyDisableExceptionHandlingAndMiddleware excludes HttpException and AuthorizationException, converting them into a swallowed 403 response (no assertForbidden/assertStatus in this Livewire version). ValidationException likewise surfaces as an error bag, not an exception. Assert the security outcome (record unchanged) instead of expectException. <!-- created=2026-08-06, last=2026-08-06 -->
§
[correction] In goldenland-realestate-v2, ticket #8 tests must not call `aiAssistAdminUser()` twice in the same test — each call registers a `Gate::before` callback denying all other users, causing AuthorizationException. Call it exactly once per test (even in nested describes, ensure only one registration). — Failed: Repeated `aiAssistAdminUser()` in permission tests caused AuthorizationException due to duplicate Gate::before callbacks. <!-- created=2026-08-07, last=2026-08-07 -->
§
[insight] In goldenland-realestate-v2, refactoring OpenAI generator's budget estimation from `count($content) - 1` to `getMedia('galleries')->count()` was an off-spec change; the correct approach is to pass `imageCount` explicitly. Always preserve existing semantics when extracting shared logic. — Failed: Review found the budget estimation change was off-spec; fixed by reverting to explicit imageCount. <!-- created=2026-08-07, last=2026-08-07 -->
§
[correction] In goldenland-realestate-v2, the code review for #8 flagged that a refactor unintentionally changed the OpenRouter budget estimation from explicit image count to counting content array items; reverted by passing `imageCount` explicitly. This is a correction on how to handle the budget. — Failed: User correction on budget estimation approach. <!-- created=2026-08-07, last=2026-08-07 -->
§
[tool-quirk] The GitHub API occasionally flaps with 'connection reset by peer' (on POST to api.github.com/graphql) and timeout errors. Retry with backoff and resume operations; user prompts retry when needed. — Failed: Transient network failure during gh issue operations for ticket 9; not a permanent failure. <!-- created=2026-08-07, last=2026-08-07 -->