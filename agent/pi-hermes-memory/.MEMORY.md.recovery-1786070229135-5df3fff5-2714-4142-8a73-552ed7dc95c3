pi-subagents: builtin subagents (worker/scout/planner) default to `inheritSkills: false` — delegated tasks must inline the skill contract, artifact paths, acceptance criteria, and validation commands. AGENTS.md delegation rules are advisory: the extension exposes a callable subagent tool but does not auto-delegate or police primary repo tools. Reliable mandatory delegation needs a primary-side runtime guard (e.g., a global extension intercepting tool_call). <!-- created=2026-08-06, last=2026-08-07 -->
§
goldenland-realestate-v2 issue-tracker workflow (user preference — missing it gets called out): keep a clean working tree, run the full test suite, commit with a clear message, then close the ticket via `gh issue close` with a summary comment; append a context pointer to parent issue #1's 'Implementation Decisions' section (commit SHA, review ledger path, closure date). The code-review skill is a mandatory pre-commit step — two-axis (Standards/Spec) review in parallel sub-agents against HEAD with the originating issue as spec; findings fixed before commit and saved to `docs/agents/reviews/<issue-number>.md`; reviewer may run async — wait for completion before committing. `.pi-subagents/` is pi-generated noise and must be gitignored. Preferred pattern: mirror existing implementations for new tickets (e.g., an existing OpenRouter agent for Complex reused for Property). <!-- created=2026-08-07, last=2026-08-07 -->
§
goldenland-realestate-v2 AI Assist pipeline (tickets #7–#10): Complex synthesis (ticket #7) finalized by extracting a shared `ContentSuggestionValidator` base for Property/Complex validators (fixed field set, plain-text title/meta, 60/160/3000 limits, restricted safe-HTML); Complex never carries `sub_description` even on failure. Ticket #8 added the bilingual English/Burmese lifecycle: `my_accepted_at`/`my_error` columns on `content_suggestions`, `hasBurmeseTranslation()`/`isBurmeseAccepted()` helpers, `ContentTranslationAgent::translate(array $english, Property|Complex $target): array` → OpenRouter impl; Complex title stays English-only; `title_searchable_en` written only on English acceptance. Ticket #9 added `ImageObservation` (media_id FK) + `ImageObservationService/Agent` through OpenRouter; glossary: Image Observation = supporting evidence, never verified facts. Ticket #10 generalized observations to Complex (Complex now requires selected gallery images, no logo as evidence). <!-- created=2026-08-07, last=2026-08-07 -->